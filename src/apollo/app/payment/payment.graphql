enum EPaymentMethod {
  COD
  PAYPAL
  CRYPTO
}

enum EPaymentStatus {
  PENDING
  SUCCESSFUL
  FAILED
}

type PaypalPayment {
  paypalPayerEmail: String!
  paypalCaptureId: String!
  payerId: String!
  rawResponse: Object
}

type TPaymentResponse {
  paymentId: String!
  orderId: String!
  paymentMethod: EPaymentMethod!
  totalPrice: Float!
  status: EPaymentStatus!
  userInfo: TUserInfoSnapshot!
  items: [TOrderItem]!
  txHash: String
  paypalTransaction: PaypalPayment
  codTransactionId: String
  createdAt: Timestamp!
  updatedAt: Timestamp!
}

type TListPaymentResponse {
  offset: Int!
  limit: Int!
  total: Int!
  query: [TQueryBy]!
  records: [TPaymentResponse]!
}

"""
Input types
"""
input ConfirmPaymentInput {
  orderId: String!
  paypalOrderId: String
  txHash: String
  codTransactionId: String
}

type Query {
  paymentHistory(paymentId: String!): TPaymentResponse!
  listPaymentHistory(offset: Int, limit: Int, query: [QueryByInput]): TListPaymentResponse!
  listPaymentByDate(limit: Int, startDate: Int!, endDate: Int!): [TPaymentResponse]!
}

type Mutation {
  confirmPayment(paymentInput: ConfirmPaymentInput!): TPaymentResponse!
}
