enum EOrderStatus {
  CREATED
  PENDING
  PAID
  CANCELLED
  COMPLETED
  FAILED
}

type TUserInfoSnapshot {
  name: String!
  address: String!
  phoneNumber: String!
  email: String!
}

type TOrderItem {
  image: String!
  name: String!
  amount: Int!
  price: Float!
  note: String
  discountPercent: Float
  selectedOptions: [TItemOption!]!
}

type TCreateOrderResponse {
  orderId: String!
  transactionId: String
  paypalApproveUrl: String
  cryptoPaymentProof: TCryptoPaymentProof
  createdAt: Timestamp!
  updatedAt: Timestamp!
}

type TOrderResponse {
  orderId: String!
  transactionId: String
  userInfoSnapshot: TUserInfoSnapshot!
  items: [TOrderItem!]!
  totalPrice: Float!
  paymentMethod: EPaymentMethod!
  orderStatus: EOrderStatus!
  createdAt: Timestamp!
  updatedAt: Timestamp!
}

type TListOrderResponse {
  offset: Int!
  limit: Int!
  total: Int!
  query: [TQueryBy]!
  records: [TOrderResponse]!
}

"""
Server-signed payment proof for on-chain crypto payment.
Pass these fields to NgooPayment.payOrder() on the smart contract.
"""
type TCryptoPaymentProof {
  orderId: String!
  orderIdHash: String!
  amount: String!
  amountDisplay: String!
  nonce: String!
  deadline: Int!
  signature: String!
  contractAddress: String!
  chainId: Int!
  payerAddress: String!
}

"""
Input types
"""
input UserInfoSnapshotInput {
  name: String!
  address: String!
  phoneNumber: String!
  email: String!
}

input OrderItemInput {
  itemId: String!
  amount: Int!
  note: String
  selectedOptions: [ItemOptionInput!]
}

input CreateOrderInput {
  items: [OrderItemInput!]!
  paymentMethod: EPaymentMethod!
  userInfo: UserInfoSnapshotInput!
  returnUrl: String!
  cancelUrl: String!
}

type Query {
  getUserOrder(orderId: String!): TOrderResponse!
  getAllOrder(orderId: String!): TListOrderResponse!
}

type Mutation {
  createOrder(input: CreateOrderInput!): TCreateOrderResponse!
}
