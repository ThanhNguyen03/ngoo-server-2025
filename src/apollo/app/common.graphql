enum ESort {
  asc
  desc
}

type TQueryBy {
  column: String
  order: EOrder
}

input QueryByInput = {
  column: String
  order: EOrder
}

input PaginationInput {
  offset: Int
  limit: Int
  query: [TQueryBy]
}
