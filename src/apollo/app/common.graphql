scalar JSON
scalar DateTime

enum ESort {
  asc
  desc
}

type TQueryBy {
  column: String
  sort: ESort
}

input QueryByInput {
  column: String
  sort: ESort
}

input PaginationInput {
  offset: Int
  limit: Int
  query: [TQueryBy]
}
